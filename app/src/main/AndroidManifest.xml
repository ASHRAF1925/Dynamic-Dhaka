<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dynamic_dhaka">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dynamic_Dhaka">
        <activity android:name=".balance_add"></activity>
        <activity android:name=".counter_master_first_menu" />
        <activity android:name=".assistent_first_menu" />
        <activity android:name=".driver_first_menu" />
        <activity android:name=".check_schedule" />
        <activity android:name=".staff_login" />
        <activity android:name=".Ticket_buying" />
        <activity android:name=".pass_shoe_route" />
        <activity android:name=".Add_Schedule" />
        <activity android:name=".Bus_addition" />
        <activity android:name=".counter_master_addition" />
        <activity android:name=".assistant_addition" />
        <activity android:name=".driver_addition" />
        <activity android:name=".Staff_Type" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Route_show"
            android:label="@string/title_activity_route_show" />
        <activity android:name=".Location_view_info" />
        <activity android:name=".Add_location" />
        <activity android:name=".Auth_menu" />
        <activity android:name=".Passenger_main_menu" />
        <activity android:name=".Pass_login_page" />
        <activity android:name=".Pass_signup" />
        <activity android:name=".Auth_first_menu" />
        <activity android:name=".Staf_first_menu" />
        <activity android:name=".Pass_first_menu" />
        <activity android:name=".User_type_menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>